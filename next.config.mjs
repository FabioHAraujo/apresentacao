/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['pocketbase.flecksteel.com.br'], // Adicione seu domínio aqui
    },
  };
  
  export default nextConfig;
  